spring.datasource.platform=postgres
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/health_tracker?currentSchema=public?user=health_tracker&password=health_tracker}
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# App Properties
healthTracker.app.jwtSecret= healthTrackerSecretKey
healthTracker.app.jwtExpirationMs= 86400000

server.port=${PORT:8080}
